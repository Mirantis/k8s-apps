apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "jenkins-test-%s" .Release.Name | trunc 55 | trimSuffix "-" }}
data:
  run.sh: |-
    @test "Testing Jenkins UI is accessible" {
      curl --retry 30 --retry-delay 10 {{ template "jenkins.master-address" . }}/login
    }
